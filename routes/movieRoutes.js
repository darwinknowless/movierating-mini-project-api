const express = require("express");

// Import validator

// Import controller

// Import auth (middleware)

// Make router
const router = express.Router();

// Exports router
module.exports = router;
